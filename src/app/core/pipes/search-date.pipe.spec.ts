import { SearchDatePipe } from './search-date.pipe';

describe('SearchDatePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchDatePipe();
    expect(pipe).toBeTruthy();
  });
});
